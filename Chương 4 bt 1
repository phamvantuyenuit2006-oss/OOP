  /* Bài  1: Thiết kế lớp điểm gồm 2 thuộc tính x, y (int) và các phương thức thiết lập/nhập/ xuất. */

import java.util.Scanner;

public class Diem {

    private int x;
    private int y;

    // Constructor không tham số
    public Diem() {
        x = 0;
        y = 0;
    }

    // Constructor có tham số
    public Diem(int x, int y) {
        this.x = x;
        this.y = y;
    }

    // Phương thức nhập
    public void nhap() {
        Scanner sc = new Scanner(System.in);
        System.out.print("Nhập hoành độ x: ");
        x = sc.nextInt();
        System.out.print("Nhập tung độ y: ");
        y = sc.nextInt();
    }

    // Phương thức xuất
    public void xuat() {
        System.out.println("(" + x + ", " + y + ")");
    }

    // Getter & Setter
    public int getX() {
        return x;
    }

    public void setX(int x) {
        this.x = x;
    }

    public int getY() {
        return y;
    }

    public void setY(int y) {
        this.y = y;
    }

    // Hàm main để kiểm tra
    public static void main(String[] args) {
        Diem d1 = new Diem();
        d1.nhap();
        System.out.print("Điểm vừa nhập: ");
        d1.xuat();

        Diem d2 = new Diem(3, 4);
        System.out.print("Điểm d2: ");
        d2.xuat();
    }
}
